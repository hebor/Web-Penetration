# PHP文件管理

PHP的文件管理模块由多个功能组合而成，包括上传、下载、删除、编辑、包含等功能。先通过开发者视角观察如何实现功能，在从攻击者视角观察如何进行测试

## 一、文件上传

```
目录树：
demo01/
├── upload/    //用于存放上传文件的目录
├── upload.php
└── upload.html
```

### 1.1 功能实现

单纯的文件上传功能实现比较简单，一个前端页面、一个逻辑处理即可

1. 编辑`upload.html`文件。HTML代码中没有太多逻辑相关内容，仍通过AI生成即可

   ```HTML
   <!DOCTYPE html>
   <html lang="zh-CN">
   <head>
       <meta charset="UTF-8">
       <title>文件上传页面</title>
       <style>
           /* 整体背景渐变 */
           body {
               margin: 0;
               height: 100vh;
               display: flex;
               flex-direction: column;
               justify-content: center;
               align-items: center;
               background: linear-gradient(135deg, #6a11cb, #2575fc); /* 紫色到蓝色渐变 */
               font-family: Arial, sans-serif;
           }
   
           /* 页面标题 h2 放在 div 上方，居中 */
           h1 {
               color: white;
               margin-bottom: 20px;
               text-align: center;
           }
   
           /* 上传容器居中显示，文字左对齐 */
           .upload-container {
               background-color: rgba(255, 255, 255, 0.95);
               padding: 30px;
               border-radius: 10px;
               box-shadow: 0 0 15px rgba(0,0,0,0.2);
               width: 350px;
               text-align: left; /* div 内文字左对齐 */
           }
   
           /* 表单元素样式 */
           label {
               display: block;
               margin-bottom: 10px;
               font-weight: bold;
           }
   
           input[type="file"] {
               margin-bottom: 20px;
               width: 100%;
           }
   
           input[type="submit"] {
               padding: 10px 20px;
               font-size: 16px;
               cursor: pointer;
               border: none;
               border-radius: 5px;
               background-color: #4CAF50;
               color: white;
           }
   
           input[type="submit"]:hover {
               background-color: #45a049;
           }
       </style>
   </head>
   <body>
   
   <!-- 页面标题 -->
   <h1>上传文件</h1>
   
   <!-- 上传容器 -->
   <div class="upload-container">
       <!--此处需要将表单提交的数据转交给upload.php文件-->
       <form action="upload.php" method="post" enctype="multipart/form-data">
           <label for="file">选择文件：</label>
           <input type="file" name="file_name" id="file_id" required>
           <br>
           <input type="submit" value="上传">
       </form>
   </div>
   
   </body>
   </html>
   ```

2. 编辑`upload.php`文件，对上传的文件进行逻辑处理

   $\_FILES是PHP中一个预定义的超全局变量，用于在上传文件时从客户端接收文件，并将其保存到服务器上。它是一个包含上传文件信息的数组，包括文件名、类型、大小、临时文件名等信息。

   - $\_FILES\["表单值"\]\["name"\]：获取上传文件原始名称。\["表单值"\]这个值取决于HTML的"name"元素的值；\["name"\]则是固定格式
   - $\_FILES\["表单值"\]\["type"\]：获取上传文件MIME类型
   - $\_FILES\["表单值"\]\["size"\]：获取上传文件字节单位大小
   - $\_FILES\["表单值"\]\["tmp_name"\]：获取上传的临时副本文件名
   - $\_FILES\["表单值"\]\["error"\]：获取上传时发生的错误代码

   - `move_uploaded_file()`：将上传的文件移动到指定位置的函数

   ```php
   <?php
   $name = $_FILES["file_name"]["name"];    //获取上传文件原始名称
   $type = $_FILES["file_name"]["type"];
   $size = $_FILES["file_name"]["size"];    //默认显示单位为Bytes
   $tmp_name = $_FILES["file_name"]["tmp_name"];
   $error = $_FILES["file_name"]["error"];
   
   //通过打印可以观察到$_FILES对文件上传的判断
   //echo($name."<br />");
   //echo($type."<br />");
   //echo($size."<br />");
   //echo($tmp_name."<br />");
   //echo($error."<br />");
   
   //将上传到临时目录中的文件移动到指定位置
   if (move_uploaded_file($tmp_name, 'upload/'.$name)) {
       echo('<script>alert("文件上传成功")</script>');
   }
   ?>
   ```

   顺利的话，到此为止文件的上传功能已经实现，但在配置的过程中可能还会遇到一些问题，比如Nginx是否启用了上传文件功能、创建的upload目录是否具备可写权限等，我的phpstudy都没有遇见这些问题，因此不做笔录

3. 文件上传测试成功

   ![文件上传-1.png](../image/Part_3/文件上传-1.png)

   ![文件上传-2](../image/Part_3/文件上传-2.png)

### 1.2 条件过滤

文件上传功能本身不具备安全防护能力，为了避免攻击者通过上传恶意文件，导致服务器被入侵、数据泄漏等问题，实现文件上传功能后还需要配置条件过滤。PHP的文件上传过滤有3种常见的方式：黑名单过滤、白名单过滤、文件类型过滤

注：所有的功能逻辑的修改都在`upload.php`文件中调整，除非单独声明，否则下文中不会重复提起`upload.html`页面

#### 1.2.1 黑名单过滤

黑名单过滤机制指的是不允许上传特定后缀名的文件，这种方式非常考验开发者的攻防经验，如果开发者没有考虑到所有后缀变形的文件，仍会被攻击者直接绕过黑名单机制，直接上传恶意文件

1. 黑名单示例代码

   ```php
   <?php
   $name = $_FILES["file_name"]["name"];    //获取上传文件原始名称
   $type = $_FILES["file_name"]["type"];
   $size = $_FILES["file_name"]["size"];    //默认显示单位为Bytes
   $tmp_name = $_FILES["file_name"]["tmp_name"];
   $error = $_FILES["file_name"]["error"];
   
   $black_ext = array('php', 'asp', 'jsp', 'aspx');    //定义禁止上传的文件后缀名
   $split_array_result = explode(".", $name);    //explode()函数用于按特定字符分割出上传的文件名称
   $exts = end($split_array_result);    //通过end()函数取数组的最后一个元素，即上传的文件的后缀名
   if (in_array($exts, $black_ext)) {
       echo("非法文件");
   } else {
       if (move_uploaded_file($tmp_name, 'upload/'.$name)) {
           echo('<script>alert("文件上传成功")</script>');
       } else {
           echo('<script>alert("文件上传失败，错误码：'. $error .'")</script>');
       }
   }
   ?>
   ```

2. 上传变种后缀文件

   ![文件上传-3](../image/Part_3/文件上传-3.png)

3. 直接访问上传的文件。能否访问成功取决于服务端的环境配置，phpstudy的nginx配置似乎默认无法识别`.php5`后缀文件


#### 1.2.2 白名单过滤

白名单过滤机制与黑名单过滤机制相反，只有明确允许上传的文件后缀名，文件上传才会成功，从机制上来看，白名单就比黑名单更加安全。但是，从运维人员的角度来看，白名单机制的工作量更大

1. 白名单示例代码

   ```php
   <?php
   $name = $_FILES["file_name"]["name"];    //获取上传文件原始名称
   $type = $_FILES["file_name"]["type"];
   $size = $_FILES["file_name"]["size"];    //默认显示单位为Bytes
   $tmp_name = $_FILES["file_name"]["tmp_name"];
   $error = $_FILES["file_name"]["error"];
   
   $white_ext = array('png', 'jpg', 'jpeg', 'gif');    //定义允许上传的文件后缀名
   $split_array_result = explode(".", $name);    //explode()函数用于按特定字符分割出上传的文件名称
   $exts = end($split_array_result);    //通过end()函数取数组的最后一个元素，即上传的文件的后缀名
   if (in_array($exts, $white_ext)) {
       if (move_uploaded_file($tmp_name, 'upload/'.$name)) {
           echo('<script>alert("文件上传成功")</script>');
       } else {
           echo('<script>alert("文件上传失败，错误码：'. $error .'")</script>');
       }    
   } else {
       echo("非法文件");
   }
   ```

2. 上传变种后缀文件

   ![文件上传-4](../image/Part_3/文件上传-4.png)

3. 根据白名单代码逻辑，文件上传失败

   ![文件上传-5](../image/Part_3/文件上传-5.png)

#### 1.2.3 MIME过滤

在http请求和响应报文中，会通过`Content-Type`字段标识请求体或响应体中的数据类型，以便于对端能够正确解析和处理数据。MIME过滤机制就是通过对`Content-Type`字段的值进行校验，判断上传的文件类型是否合法

1. MIME示例代码

   ```php
   <?php
   $name = $_FILES["file_name"]["name"];    //获取上传文件原始名称
   $type = $_FILES["file_name"]["type"];
   $size = $_FILES["file_name"]["size"];    //默认显示单位为Bytes
   $tmp_name = $_FILES["file_name"]["tmp_name"];
   $error = $_FILES["file_name"]["error"];
   
   $allow_type = array('image/png', 'image/jpg', 'image/jpeg', 'image/gif');
   if (in_array($type, $allow_type)) {    //根据文件类型进行判断
       if (move_uploaded_file($tmp_name, 'upload/'.$name)) {
           echo('<script>alert("文件上传成功")</script>');
       } else {
           echo('<script>alert("文件上传失败，错误码：'. $error .'")</script>');
       }    
   } else {
       echo("非法文件".$type);    //文件类型非法时输出上传的数据类型
   }
   ?>
   ```

2. 上传变种后缀文件，根据代码逻辑判断为非法文件，输出变种后缀文件的数据类型

   ![文件上传-6](../image/Part_3/文件上传-6.png)

3. 通过抓包手动修改MIME数据类型后，文件上传成功

   ![文件上传-7](../image/Part_3/文件上传-7.png)

   可见，MIME过滤机制只校验`Content-Type`值，不校验文件内容，容易被伪造数据类型

条件过滤不能仅凭某一种机制来保障安全性，一般会使用逻辑更加严谨的组合性过滤，每一种过滤机制只针对某一类型的数据进行筛选

## 二、文件显示

```php
目录树：
demo01/
├── upload/    //用于存放上传文件的目录
├── upload.php
├── upload.html
└── file-manage.php    //文件显示页面
```

文件显示功能的逻辑，简单描述为：打开一个目录，对目录下的所有目标进行遍历，判断目标类型是否为目录，当目标类型为目录时，继续进入该子目录继续进行遍历判断。文件显示功能会用到几个函数

| 函数      | 作用                       | 简要说明                                                     |
| --------- | -------------------------- | ------------------------------------------------------------ |
| opendir() | 用于打开一个目录           | 执行成功时会打开指定路径的目录，并返回一个目录Resource类型的句柄；<br />执行失败时会返回False |
| readdir() | 用于从打开的目录中读取条目 | readdir()函数需要一个目录句柄，即opendir()返回的资源，每成功调用一次readdir()都会返回目录中的下一个文件或目录；<br />目录读取完成后、或调用失败时，返回False |
| is_dir()  | 判断路径是否为目录         | 返回布尔值 True 或 False                                     |

### 2.1 功能实现

1. 文件显示功能代码

   ```php
   <?php
   $www = $_GET['path'] ?? './';
   
   function show_file($dir) {
       $root_dir = opendir($dir);    //opendir()函数用于打开当前目录，执行成功时返回一个resource类型句柄、执行失败时返回False
       while (($target = readdir($root_dir)) !== False) {    //判断readdir()的执行结果，readdir()执行结果不为False时，为$target变量进行赋值
           if (is_dir($target)) {    //对$target进行判断是否为目录
               echo("<a href='?path=$target'>$target</a><br />");
           } else {
               echo($target."<br />");
           }
       }
   }
   show_file($www);
   ?>
   ```

2. 测试文件显示功能

   ![文件显示-1](../image/Part_3/文件显示-1.png)

3. 在未对用户输入的参数进行校验的情况下，对前端参数进行修改，通过目录遍历漏洞可以获取服务端的所有文件信息

   ![文件显示-2](../image/Part_3/文件显示-2.png)

4. 通过代码逻辑可以实现对用户输入的特殊字符做校验，大部分中间件也直接提供了类似的功能，PHP在`php.ini`配置文件中提供了一个参数`open_basedir`，用于限制用户能够访问的路径。修改`php.ini`配置文件后重启phpstudy服务，再次测试目录遍历失败

   ```shell
   vim /Applications/phpstudy/Extensions/php/php7.3.11/php.ini
   open_basedir = /Applications/phpstudy/WWW/demo01/    #这一行默认注释，取消注释后，添加访问限制路径
   ```

   